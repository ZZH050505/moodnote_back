<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.demo.mapper.HappyEventMapper">

<!--    property指的是实体类字段，column指的是数据库字段-->
    <insert id="insert" useGeneratedKeys="true" keyProperty="eventID"  keyColumn="EventID">
        insert into happyevent(UserID, Content, IsPublic, CreateTime, UpdateTime)
        values (#{userID}, #{content}, #{isPublic}, #{createTime}, #{updateTime})
    </insert>
    <update id="updateEvent">
        update happyevent set
        Content=#{content},
        IsPublic=#{isPublic},
        UpdateTime= #{updateTime}
        where EventID=#{eventID}
        and UserID=#{userID}

    </update>
</mapper>